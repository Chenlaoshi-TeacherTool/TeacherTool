<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#356e57">
  <title><%= title %> - Chen Laoshi's Teaching Toolkit</title>
  <%- include('../partials/seo', { pageDescription: 'This shared book link has expired.', noindex: true }) %>
  <link rel="stylesheet" href="/stylesheets/site-theme.css">
  <link rel="stylesheet" href="/low-prep/book-generator/book-player.css?v=1">
</head>
<body class="book-player-page book-player-gate-page">
  <main class="book-player-gate">
    <h1><%= t('bookPlayer.expiredTitle') %></h1>
    <p><%= t('bookPlayer.expiredMessage') %></p>
    <a class="btn" href="/teaching-tools/book-generator"><%= t('bookGenerator.title') %></a>
  </main>
</body>
</html>
