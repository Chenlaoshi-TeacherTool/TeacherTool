<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('../../partials/adsense') %>
  <title>Seasons & Weather</title>
  <%- include('../../partials/seo', { pageDescription: 'Teach seasons and weather in Chinese with classroom games, songs, vocabulary, and ready-to-use activities.' }) %>
  <style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f8f9fa;
        color: #333;
        line-height: 1.6;
    }

    /* 🌈 HEADER */
    .header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 40px 20px;
        text-align: center;
        position: relative;
    }

    .header h1 {
        margin-bottom: 10px;
    }

    .header p {
        opacity: 0.9;
        font-size: 14px;
    }

    .back-btn {
        position: absolute;
        top: 20px;
        left: 20px;
        background-color: rgba(255, 255, 255, 0.2);
        border: 1px solid white;
        color: white;
        padding: 10px 15px;
        text-decoration: none;
        border-radius: 5px;
    }

    .back-btn:hover {
        background-color: rgba(255, 255, 255, 0.3);
    }

    /* 🔍 SEARCH */
    .search-container {
        text-align: center;
        margin: 20px;
    }

    #searchBox {
        width: 80%;
        max-width: 500px;
        padding: 12px;
        border-radius: 8px;
        border: 1px solid #ccc;
        font-size: 16px;
    }

    /* 📦 GRID */
    .tools-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 20px;
        padding: 40px 20px;
        max-width: 1200px;
        margin: 0 auto;
    }

    /* 🧩 CARD */
    .tool-card {
        background: white;
        border-radius: 12px;
        padding: 25px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
        transition: transform 0.3s, box-shadow 0.3s;
    }

    .tool-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
    }

    .tool-card:has(.btn:disabled) {
        background: #f3f4f6;
        box-shadow: none;
        opacity: 0.68;
    }

    .tool-card:has(.btn:disabled):hover {
        transform: none;
        box-shadow: none;
    }

    .tool-card h3 {
        color: #667eea;
        margin-bottom: 10px;
        font-size: 18px;
    }

    .tool-card p {
        margin-bottom: 15px;
        font-size: 14px;
    }

    /* 🎯 BUTTON */
    .btn {
        background-color: #667eea;
        color: white;
        padding: 10px 18px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        font-size: 14px;
    }

    .btn:hover {
        background-color: #5a67d8;
    }

    .btn:disabled {
        background-color: #aab1bf;
        color: #f8fafc;
        cursor: not-allowed;
    }

    /* 🌤️ OPTIONAL: Weather color tags（小亮点） */
    .tag {
        display: inline-block;
        font-size: 12px;
        padding: 4px 8px;
        border-radius: 5px;
        margin-bottom: 10px;
        background-color: #eef2ff;
        color: #667eea;
    }

    /* 📱 RESPONSIVE */
    @media (max-width: 768px) {
        .tools-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
  <link rel="stylesheet" href="/stylesheets/site-theme.css">
</head>
<body>
  <div class="header">
    <a href="/theme-activities" class="back-btn">⬅ <%= t('themeActivitiesWeather.back') %></a>
    <h1>🌦️ <%= t('themeActivitiesWeather.title') %></h1>
    <p><%= t('themeActivitiesWeather.subtitle') %></p>
</div>

<div class="tools-grid">

    <div class="tool-card">
        <div class="tag"><%= t('themeActivitiesWeather.tagVocabulary') %></div>
        <h3><%= t('themeActivitiesWeather.vocabTitle') %></h3>
        <p><%= t('themeActivitiesWeather.vocabDesc') %></p>
        <p><strong><%= t('themeActivitiesWeather.vocabBestForStrong') %></strong> <%= t('themeActivitiesWeather.vocabBestForText') %></p>
    </div>

    <div class="tool-card">
        <div class="tag"><%= t('themeActivitiesWeather.tagPractice') %></div>
        <h3><%= t('themeActivitiesWeather.practiceTitle') %></h3>
        <p><%= t('themeActivitiesWeather.practiceDesc') %></p>
        <p><strong><%= t('themeActivitiesWeather.practiceTipStrong') %></strong> <%= t('themeActivitiesWeather.practiceTipText') %></p>
    </div>

    <div class="tool-card">
        <div class="tag"><%= t('themeActivitiesWeather.tagGame') %></div>
        <h3><%= t('themeActivitiesWeather.gameTitle') %></h3>
        <p><%= t('themeActivitiesWeather.gameDesc') %></p>
        <p><strong><%= t('themeActivitiesWeather.gameTimeStrong') %></strong> <%= t('themeActivitiesWeather.gameTimeText') %></p>
    </div>

    <div class="tool-card">
        <div class="tag"><%= t('themeActivitiesWeather.tagSong') %></div>
        <h3><%= t('themeActivitiesWeather.songTitle') %></h3>
        <p><%= t('themeActivitiesWeather.songDesc') %></p>
        <a href="/theme-activities/weather/songs" class="btn"><%= t('themeActivitiesWeather.openSongNotes') %></a>
    </div>

        <div class="tool-card">
        <div class="tag"><%= t('themeActivitiesWeather.tagHandsOn') %></div>
        <h3><%= t('themeActivitiesWeather.craftsTitle') %></h3>
        <p><%= t('themeActivitiesWeather.craftsDesc') %></p>
        <p><strong><%= t('themeActivitiesWeather.craftsMaterialsStrong') %></strong> <%= t('themeActivitiesWeather.craftsMaterialsText') %></p>
    </div>
</div>
<script src="/javascripts/account-coming-soon.js"></script>
  <script src="/javascripts/milk-tea-footer.js"></script>
</body>
</html>
